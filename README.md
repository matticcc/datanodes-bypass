# datanodes-bypass
completely made with AI, i needed it so i thought i might share it
python script made to automate datanodes wait, processes 3 links per batch

Requirements:
pip install -r requirements.txt 

feel free to fork this, or do whatever you want with it
